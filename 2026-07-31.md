# 学习日志 2026-07-31

- 学习者：Isaac
- 学习方向：Java
- 目标学习时长：175 分钟
- 实际完成时长：215 分钟
- 完成情况：3 / 3（100%）

## 今日任务

- [x] 主课程学习（100 分钟）
  - 完成标准：完成今天计划的章节，并用自己的话写 3 条要点
- [x] 编程练习（55 分钟）
  - 完成标准：提交至少一个可以独立运行的练习文件
- [x] 英语或技术阅读（60 分钟）
  - 完成标准：记录 5 个新词或写一段内容摘要

## 今天学会了什么

你今天主要学习了下面这些内容：

## 1. JDK 的安装和配置

你成功安装了：

```text
Eclipse Temurin JDK 25.0.4
```

并配置了：

- `Path`：让电脑能够找到 `java` 和 `javac`。
- `JAVA_HOME`：记录 JDK 的安装位置。

你还成功运行了：

```text
java -version
javac -version
```

## 2. 环境变量

你理解了：

- 环境变量是电脑保存的一些公共设置。
- Path 用来保存一系列路径。
- 多个路径之间使用英文分号 `;` 分隔。
- 把 JDK 的 `bin` 目录加入 Path 后，可以在任意目录使用 Java 命令。

## 3. Java 的编译和运行

你学习了两个重要命令：

```text
javac HelloWorld.java
```

作用：编译 Java 源代码，生成：

```text
HelloWorld.class
```

然后使用：

```text
java HelloWorld
```

运行编译后的程序。

完整过程：

```text
.java源文件
    ↓ javac编译
.class字节码
    ↓ JVM运行
程序结果
```

## 4. Java 为什么能跨平台

Java 的跨平台能力来自 JVM，而不是环境变量。

```text
环境变量 → 帮电脑找到Java工具
JVM       → 让Java字节码能在不同系统运行
```

## 5. Eclipse 开发工具

你学习了 Eclipse 的作用：

- 创建 Java 项目。
- 编写 Java 代码。
- 自动编译和运行程序。
- 在 Console 中显示结果。
- 查找和调试错误。

Eclipse 是开发工具，Java 是编程语言，JDK 是 Java 工具箱。

## 6. Java 英文题目

你练习了 SJSU CS 46A 风格的英文题目，学习了这些表达：

- `source file`：源文件
- `bytecode file`：字节码文件
- `compile`：编译
- `run`：运行
- `integer`：整数
- `return a value`：返回一个值
- `environment variable`：环境变量
- `largest value`：最大值

## 7. Java 编程练习

你完成了两个程序：

- 使用数组和 `for` 循环寻找最大值。
- 创建 `Dog` 类，学习属性、构造方法、对象和成员方法。

你还发现了一个重要规则：

> 一个文件通常只放一个 `public class`，文件名必须和这个类名相同。

例如：

```text
FindLargest.java → public class FindLargest
Dog.java         → public class Dog
```

今天最重要的成果是：**Java 环境已经配置成功，而且你已经能够理解 Java 程序从编写、编译到运行的完整过程。**

## 遇到的问题

你今天主要遇到了这些问题：

1. 命令窗口找不到 Java

输入：

```text
java
javac
```

电脑显示：

```text
is not recognized
```

原因：JDK 最初没有正确加入 Path 环境变量。

解决方法：

- 安装 Eclipse Temurin JDK。
- 启用 `Modify PATH variable`。
- 设置 `JAVA_HOME`。
- 关闭旧命令窗口，再打开新窗口。

最后成功显示：

```text
javac 25.0.4
```

2. 不知道 JDK 是否已经安装

电脑能搜索到类似：

```text
jdk.management.agent
```

但它只是 IDEA 自带 JBR 中的文件夹，不代表命令窗口能够使用 JDK。

后来确认独立 JDK 安装在：

```text
C:\Program Files\Eclipse Adoptium\jdk-25.0.4.7-hotspot
```

3. `javac` 显示很多问号

只输入：

```text
javac
```

后出现帮助信息，其中的中文变成了 `????`。

原因：命令窗口的字符编码不匹配。

可以先输入：

```text
chcp 65001
```

这不会影响 Java 编译器本身。

4. Path 题目中的标点看错

题目考查多个路径之间使用什么符号分隔。

正确的是英文分号：

```text
;
```

不是英文逗号：

```text
,
```

5. 混淆环境变量和跨平台

你一开始认为配置环境变量是为了实现跨平台。

实际上：

```text
环境变量 → 帮电脑找到java和javac
JVM       → 帮助Java程序跨平台运行
```

6. 不清楚编译和运行的区别

你对下面两个命令的区别还不熟悉：

```text
javac HelloWorld.java
```

用于编译源代码。

```text
java HelloWorld
```

用于运行编译后的程序。

7. 英文 Java 词汇不熟悉

例如：

- `integer`：整数
- `compile`：编译
- `bytecode`：字节码
- `return a value`：返回一个值

你把 `integer` 写成了 `integrate`，需要继续熟悉英文术语。

8. 两个 Java 程序混在了一起

你把 `FindLargest` 和 `Dog` 两个 `public class` 粘贴在同一段代码中，导致整体不能编译。

正确做法是分别保存：

```text
FindLargest.java
Dog.java
```

总体上，今天最大的问题是 **JDK 环境配置**，但这个问题已经成功解决。接下来需要重点巩固编译、运行和基础英文术语。

## 明日计划

明天计划把java的编程基础学会

## 学习成果

提交日报时，请同时附上当天产生的：

- 代码文件
- 练习题
- 学习笔记
- 项目链接
- 必要的少量截图
